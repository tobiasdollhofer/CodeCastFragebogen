package log;

/**
 * different events which can be detected and written to logs
 */
public enum EventType {
    FILE_OPENED, FILE_FOCUSES, FILE_CLOSED, CARET_POSITION_CHANGED, SELECTION_CHANGED, STARTUP, QUESTIONAIRE_CLICKED, TEST_CLICKED
}
